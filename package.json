{
  "name": "felipeares",
  "version": "1.0.0",
  "description": "felipe portfolio",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "aws s3 sync ./ s3://felipeares/ --exclude '*.DS_Store' --exclude '.gitignore' --exclude '.git/*' --exclude 'README.md' --exclude 'package.json' --profile felipeares"
  },
  "author": "felipeares",
  "license": "ISC",
  "devDependencies": {
    "express": "^4.16.2"
  }
}
